var x= 200;
var vel = 10;
function setup(){
    createCanvas(400,400);
}
function draw(){
    background(0);
    ellipse(x,200,100,100);
    fill(0,0,255);
    x=x+vel;
    if(x==350){
        vel=vel*-1;
    }
    if(x<50){
        vel=vel*-1;
    }
}
